<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= book.title %> Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: white;
            color: black;
        }
        .book-info {
            margin: 20px;
            padding: 10px;
            border: 1px solid orange;
            background-color: #f4f4f4;
        }
        .book-info h2 {
            color: orange;
        }
        label {
            display: block;
            margin-top: 10px;
            font-weight: bold;
        }
        input, textarea {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        button {
            background-color: orange;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        button:hover {
            background-color: #e67e22;
        }
    </style>
</head>
<body>

    <div class="book-info">
        <h2>Edit Book Details for <%= book.title %></h2>

        <!-- Form to update book details -->
        <form action="/book/update/<%= book.id %>" method="POST">
            <label for="title">Book Title</label>
            <input type="text" id="title" name="title" value="<%= book.title %>" required>

            <label for="author">Author</label>
            <input type="text" id="author" name="author" value="<%= book.author %>" required>

            <label for="genre">Genre</label>
            <input type="text" id="genre" name="genre" value="<%= book.genre %>" required>

            <label for="date">Date Published</label>
            <input type="date" id="date" name="date" value="<%= book.date %>" required>

            <label for="isbn">ISBN</label>
            <input type="text" id="isbn" name="isbn" value="<%= book.isbn %>" required>

            <label for="path">Image Path</label>
            <input type="text" id="path" name="path" value="<%= book.path %>" required>

            <button type="submit">Update Book</button>
        </form>
    </div>

</body>
</html>
